import { supabase } from '@/lib/supabaseClient'; export async function insertWithUser(table:string, values:any){const {data:{user},error}=await supabase.auth.getUser(); if(error) return {error}; return supabase.from(table).insert({...values,user_id:user?.id});}
