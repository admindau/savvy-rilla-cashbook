"use client";import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabaseClient';export default function P(){const[r,setR]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>{if(!data.session) location.href='/auth'; else setR(true);});},[]); if(!r) return <div className='p-8'>Loading…</div>; return <div className='p-8'>Dashboard placeholder — wire your existing pages here.</div>}
